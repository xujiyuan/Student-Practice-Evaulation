const CalculationService = {
  add(a: number, b: number): number {
    return 0; // this needs to be implemented
  },
  moinus(a: number, b: number): number {
    return 0; // this needs to be implemented
  },
};

export { CalculationService };
